import React from 'react';
import classes from 'Mask.module.css';

function mask(props) {
    return (
        <div className={classes.Mask}>
            
        </div>
    );
};

export default mask;